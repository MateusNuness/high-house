## Agent skills

### Issue tracker

Issues and specs live as markdown files locally in `.scratch/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Using standard canonical triage roles. See `docs/agents/triage-labels.md`.

### Domain docs

Multi-context layout using multiple READMEs across the project. See `docs/agents/domain.md`.
